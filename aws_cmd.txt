
Test credentials file
Windows:
C:\> dir "%UserProfile%\.aws"

Linux: ls  ~/.aws
~/.aws/config
~/.aws/credentials

/List config list
aws configure list

aws sts get-caller-identity




test cluster
aws ecs describe-clusters --cluster <your-cluster>


Describe cluster default
--->aws ecs describe-clusters


ap-northeast-1

/Call lambda function
aws lambda invoke --profile default --region us-west-2 --function-name lambda-aig-dev-schedule-event-processor --invocation-type Event --payload file://payload.json response.json




Check aws secretsmanager
aws secretsmanager get-secret-value --secret-id TERRAFORM_ROLES

aws ec2 describe-instances

 -Generate session-token
 aws sts get-session-token --duration-seconds 129600


aws s3 cp <LocalPath> <S3Uri> or <S3Uri> <LocalPath> or <S3Uri> <S3Uri>


############Get SSM value

aws ssm get-parameters --names dev-central-xe-external-dns  --with-decryption --region ap-southeast-1 --query Parameters[0].Value --output text